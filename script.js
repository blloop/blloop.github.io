// Script file for interactive components

// const viewScroller = document.querySelectorAll('[data-slide]');

// viewScroller.forEach(button => {
// 	button.addEventListener('click', () => {
// 		const loc = document.querySelector(button.dataset.slide);
// 		loc.scrollIntoView({behavior: "smooth"});
// 	});
// });